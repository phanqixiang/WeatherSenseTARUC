name: weathersense_taruc_2020
description: FYP By Phan Qi Xiang and Tharshini Nadarajah

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  cloud_firestore: ^0.14.0+2
  firebase_database: ^4.0.0
  firebase_core: any
  provider: ^4.3.2+2
  mqtt_client: ^8.0.0
  flutter_svg: ^0.19.0
  page_transition: ^1.1.7+2

  flutter:
    sdk: flutter


  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  assets:
    - assets/

  uses-material-design: true

